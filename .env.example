# Copy this file to .env and fill in your actual values

# Database Configuration - CHANGE THESE FOR PRODUCTION!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ChangeThisSecurePassword123!
POSTGRES_DB=cartridge_db

# Environment Variables Template for Cartridge
# 
# INSTRUCTIONS:
# 1. Copy this file to create your local .env:
#    cp .env.example .env
#
# 2. Fill in your actual values (never commit the .env file!)
#
# 3. For production deployments with GitHub Actions:
#    - Store all secrets in GitHub Secrets (see docs/GITHUB_ACTIONS_SETUP.md)
#    - The workflow automatically creates .env on deployment
#
# 4. This template uses EXAMPLE values - replace them with your real credentials!

# =============================================================================
# API KEYS
# =============================================================================

# Steam API Key
# Get yours from: https://steamcommunity.com/dev/apikey
STEAM_API_KEY=YOUR_STEAM_API_KEY_HERE

# RAWG API Key (for game metadata and search)
# Get yours from: https://rawg.io/apidocs
RAWG_API_KEY=YOUR_RAWG_API_KEY_HERE

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ChangeThisToASecurePassword123!
POSTGRES_DB=cartridge_db

# Database Port (exposed on host)
POSTGRES_PORT=5433

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# ASP.NET Core Environment (Development, Production, Staging)
ASPNETCORE_ENVIRONMENT=Production

# Application Port (exposed on host)
APP_PORT=8081

# =============================================================================
# SECURITY NOTES
# =============================================================================
# ⚠️ IMPORTANT:
# 1. NEVER commit the .env file to Git (it's in .gitignore)
# 2. Use strong, unique passwords for POSTGRES_PASSWORD
# 3. Keep your API keys private and never share them
# 4. For production deployments, use GitHub Secrets instead of .env files
# 5. Each developer should have their own .env file with their own API keys
# 6. Rotate credentials regularly for better security
#
# 📖 For automatic deployment setup, see:
#    - docs/GITHUB_ACTIONS_SETUP.md (complete guide)
#    - docs/GITHUB_SECRETS_REFERENCE.md (secrets quick reference)
