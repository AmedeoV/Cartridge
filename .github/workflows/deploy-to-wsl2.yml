name: Deploy to WSL2

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allow manual trigger

jobs:
  deploy:
    runs-on: self-hosted  # Runs on your WSL2 runner
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to WSL2
        env:
          STEAM_API_KEY: ${{ secrets.STEAM_API_KEY }}
          RAWG_API_KEY: ${{ secrets.RAWG_API_KEY }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          POSTGRES_PORT: ${{ secrets.POSTGRES_PORT }}
          APP_PORT: ${{ secrets.APP_PORT }}
        run: |
          set -e
          
          echo "🚀 Starting deployment to WSL2..."
          
          # Navigate to WSL2 deployment directory
          cd ~/apps/cartridge
          
          echo "📦 Pulling latest changes..."
          git pull origin main
          
          echo "🔧 Creating .env file from GitHub secrets..."
          # Create .env file directly with environment variables
          cat > .env << 'ENVEOF'
STEAM_API_KEY=${STEAM_API_KEY}
RAWG_API_KEY=${RAWG_API_KEY}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=${POSTGRES_DB}
POSTGRES_PORT=${POSTGRES_PORT}
ASPNETCORE_ENVIRONMENT=Production
APP_PORT=${APP_PORT}
ENVEOF
          
          # Use envsubst to replace variables (safer than unquoted heredoc)
          export STEAM_API_KEY RAWG_API_KEY POSTGRES_USER POSTGRES_PASSWORD POSTGRES_DB POSTGRES_PORT APP_PORT
          envsubst < .env > .env.tmp && mv .env.tmp .env
          
          echo "🔍 Checking if database needs to be recreated..."
          # Check if postgres container exists and if password has changed
          if docker compose ps postgres | grep -q "Up"; then
            echo "📊 Database container is running, checking password..."
            if docker compose exec -T postgres psql -U ${POSTGRES_USER} -d ${POSTGRES_DB} -c "SELECT 1" > /dev/null 2>&1; then
              echo "✅ Database credentials are valid"
              DB_NEEDS_RESTART=false
            else
              echo "⚠️  Database password has changed, full restart required"
              DB_NEEDS_RESTART=true
            fi
          else
            echo "📊 Database container not running"
            DB_NEEDS_RESTART=true
          fi
          
          if [ "$DB_NEEDS_RESTART" = true ]; then
            echo "🛑 Performing full restart (database credentials changed)..."
            docker compose down
            docker compose up -d --build
          else
            echo "🔄 Performing rolling update (zero-downtime)..."
            docker compose build web
            
            echo "🚀 Starting new web container..."
            docker compose up -d --no-deps --build --wait web
          fi
          
          echo "⏳ Waiting for health check..."
          sleep 10
          
          echo "✅ Checking container status..."
          docker compose ps
          
          echo "📊 Deployment completed successfully!"
      
      - name: Health Check
        env:
          HEALTH_CHECK_URL: ${{ secrets.HEALTH_CHECK_URL }}
        run: |
          echo "🔍 Running health check..."
          sleep 15
          
          if [ -n "$HEALTH_CHECK_URL" ]; then
            # Try up to 5 times with 10 second delays
            for i in {1..5}; do
              if curl -f -s -o /dev/null "$HEALTH_CHECK_URL"; then
                echo "✅ Health check passed! Application is responding."
                exit 0
              else
                echo "⏳ Attempt $i/5 failed, waiting 10 seconds..."
                sleep 10
              fi
            done
            echo "❌ Health check failed after 5 attempts"
            exit 1
          else
            echo "⚠️ HEALTH_CHECK_URL not set, skipping health check"
            echo "   Set this secret to enable health checks (e.g., https://cartridge.step0fail.com)"
          fi
      
      - name: Cleanup Old Docker Images
        run: |
          cd ~/apps/cartridge
          echo "🧹 Cleaning up old Docker images..."
          docker image prune -f
          echo "✅ Cleanup completed"
      
      - name: Send Deployment Notification
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "🎉 Deployment succeeded!"
          else
            echo "❌ Deployment failed!"
          fi
